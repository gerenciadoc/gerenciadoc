# Variáveis de ambiente para o Gerenciadoc
# Copie este arquivo para o painel do Vercel em Settings > Environment Variables

# Conexão com MongoDB
MONGODB_URI=mongodb+srv://gerenciadoc:<db_password>@cluster0.vowrllh.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Substitua <db_password> pela senha real do banco de dados

# Chave secreta para JWT (autenticação)
# Gere uma chave segura para produção, por exemplo usando: 
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=gerenciadoc-production-jwt-secret-key

# Outras configurações
NODE_ENV=production
